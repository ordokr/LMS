/* Tag Cloud Styles */
.tag-cloud .tag-item {
    padding: 0.35rem 0.65rem;
    border-radius: 30px;
    text-decoration: none;
    font-size: 0.85rem;
    transition: transform 0.15s ease;
}

.tag-cloud .tag-item:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

/* Tag Filter Styles */
.tag-color-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    display: inline-block;
}

.tag-filter .list-group-item-action.active {
    background-color: #f0f0f0;
    color: var(--bs-body-color);
    border-color: rgba(0,0,0,.125);
}

.tag-filter .list-group-item-action.active .tag-name {
    font-weight: 600;
}

/* Selected Tags Styles */
.selected-tags .badge {
    font-size: 0.85rem;
    font-weight: 500;
    padding: 0.5rem 0.65rem;
}

.selected-tags .btn-close {
    font-size: 0.6rem;
}